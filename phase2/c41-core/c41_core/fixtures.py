"""Synthetic C-41 orange-mask fixture builders — stub (implemented in Task 2)."""
from __future__ import annotations

import numpy as np

DEFAULT_SEED = 42


def make_c41_negative(
    height: int = 128,
    width: int = 192,
    seed: int = DEFAULT_SEED,
    **kwargs: object,
) -> np.ndarray:
    raise NotImplementedError("Implemented in Task 2")


def make_rebate_strip(
    height: int = 128,
    width: int = 192,
    seed: int = DEFAULT_SEED,
    **kwargs: object,
) -> np.ndarray:
    raise NotImplementedError("Implemented in Task 2")
