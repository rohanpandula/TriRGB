# macOS
.DS_Store

# Python
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
*.egg-info/

# Swift
.build/
*.xcodeproj/
DerivedData/

# Local
.env
.env.*
*.secret
*.secrets
.sony-capture-fingerprint.bin
*.fingerprint.bin
*.local
*.log
.external/

# Agent harness — vendored/installed skills (re-installable from skills-lock.json)
.agents/

# Agent harness — local Claude Code state (settings.local.json, locks, worktrees)
.claude/

# Planning (gsd internal — kept local, not published)
.planning/
